all:
    vars:
        ansible_connection: docker
        ansible_python_interpreter: auto_silent
        ansible_become: false
    children:
        webservers:
            hosts:
                ansible-node1:
        appservers:
            hosts:
                ansible-node2:
